# 🌡️ Temperature Converter (Rust CLI)

A simple **command-line temperature converter** written in **Rust**, allowing users to convert between **Fahrenheit** and **Celsius**.  
This project was built as a beginner exercise to practice **loops**, **user input handling**, and **if-else logic** in Rust.

---

## 🚀 Features

- Convert **Fahrenheit → Celsius**
- Convert **Celsius → Fahrenheit**
- Loop until the user chooses to exit
- Graceful input handling and friendly prompts

---

## 🧠 Learning Goals

- Practice using `io::stdin` for reading input  
- Work with **loops (`while`, `if`, `else`)**
- Understand **parsing strings to numbers**
- Display results using formatted output



